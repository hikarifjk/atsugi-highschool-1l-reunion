---
import Layout from '../layouts/Layout.astro';

const formUrl = 'https://example.com/google-form';
const tbd = '未定（決まり次第更新）';
---

<Layout title="厚木高校1L+α 同窓会 | 開催詳細">
	<section class="card">
		<h1>開催詳細</h1>
		<dl class="event-list">
			<div>
				<dt>開催日</dt>
				<dd>未定(2026/05中旬以降)</dd>
			</div>
			<div>
				<dt>開催時間</dt>
				<dd>{tbd}</dd>
			</div>
			<div>
				<dt>開催場所</dt>
				<dd>未定(田園都市線駅周辺で探します)</dd>
			</div>
			<div>
				<dt>会費</dt>
				<dd>{tbd}</dd>
			</div>
		</dl>
		<p>詳細が確定次第、このページで更新します。</p>
		<p class="cta-note"><strong>人数把握したいため、仮出欠登録にご協力ください。</strong></p>
		<a class="form-button" href={formUrl} target="_blank" rel="noopener noreferrer">仮出欠登録(Googleフォーム)</a>
	</section>
</Layout>

<style>
	h1 {
		margin-top: 0;
		color: #8e3f23;
	}

	.card {
		background: var(--card-bg);
		border: 1px solid var(--border);
		border-radius: 14px;
		padding: 1.2rem;
		box-shadow: 0 10px 24px rgba(133, 89, 56, 0.08);
	}

	.event-list {
		margin: 0 0 1rem;
		display: grid;
		gap: 0.8rem;
	}

	dt {
		font-weight: 700;
		color: #6c3f2a;
	}

	dd {
		margin: 0.2rem 0 0;
		color: #3b2f2f;
	}

	.cta-note {
		margin-bottom: 0.5rem;
		font-size: 1.02rem;
		color: #8b2f1e;
	}

	.form-button {
		display: inline-block;
		background: linear-gradient(135deg, var(--accent) 0%, var(--accent-strong) 100%);
		color: #fff;
		text-decoration: none;
		font-weight: 700;
		padding: 0.95rem 1.6rem;
		border-radius: 12px;
		box-shadow: 0 6px 16px rgba(200, 88, 47, 0.35);
	}
</style>
